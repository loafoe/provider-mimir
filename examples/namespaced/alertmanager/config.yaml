apiVersion: alertmanager.mimir.m.crossplane.io/v1alpha1
kind: Config
metadata:
  name: example-alertmanager-config-ns
  namespace: default
spec:
  forProvider:
    # Configuration for Grafana Mimir Alertmanager
    receiver:
    - name: default
    route:
    - receiver: default
  providerConfigRef:
    kind: ClusterProviderConfig
    name: default
